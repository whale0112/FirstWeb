var other = document.getElementById('other');
        other.addEventListener('click', function(){
            var date = new Date();
            alert(date);
        });

        // body 를 다 읽고 불러오기 때문에 속도가 느릴 수 있음